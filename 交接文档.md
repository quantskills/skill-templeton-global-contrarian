# Skill-Templeton-Global-Contrarian 交接文档

## 一、项目概述

### 1.1 项目背景
本项目为 **邓普顿逆向全球价值因子**（Templeton Global Contrarian Value Factor）技能开发，基于约翰·邓普顿爵士的逆向投资理念：**在市场悲观时买入被低估的股票**。

### 1.2 因子逻辑
$$Factor = Market\_Z \times Stock\_Z$$
- **Market_Z**: 市场情绪Z-score（基于A股指数PE历史分布）
- **Stock_Z**: 个股估值Z-score（基于PE相对市场均值的标准化，取负值）

**核心思想**：市场极度悲观（Market_Z < 0）+ 个股极度低估（Stock_Z > 0，PE低于均值）→ 因子值为正 → 买入信号

### 1.3 覆盖市场
- A股（CN）：使用财务数据计算PE TTM
- 港股（HK）：使用估值指标接口获取PE
- 美股（US）：暂无数据（权限限制）

---

## 二、文件结构

```
skill-templeton-global-contrarian/
├── 开发产物/
│   ├── .env                    # 环境变量配置（账密模板）
│   ├── 生产产物/
│   │   └── 数据库.parquet      # 因子计算输出文件
│   ├── scripts/
│   │   ├── factor.py           # 核心因子计算脚本
│   │   ├── validate.py         # 验证脚本（三层验证）
│   │   └── backtest.py         # 回测脚本
│   └── 交接文档.md              # 本文件
```

---

## 三、关键问题与修复记录

### 3.1 P0级问题修复

| 问题编号 | 问题描述 | 根因 | 修复方案 | 修复位置 |
|----------|----------|------|----------|----------|
| P0-1 | 港股估值数据重复 | `get_stock_mktfin_indicator`返回多日期数据 | 添加`drop_duplicates(subset=["ts_code"])` | factor.py#L208 |
| P0-2 | 信号方向翻转 | PE越高因子值越高，但PE高是高估应卖出 | `stock_z_score = -(pe - mean) / std` | factor.py#L361 |
| P0-3 | 负PE股混入buy组 | 亏损股PE为负，被误判为低估 | 添加`market_df["pe"] > 0`过滤 | factor.py#L353 |

### 3.2 技术难点解决

| 难点 | 解决方案 |
|------|----------|
| A股日线无market_cap | 使用`get_share_float`获取总股本，计算`市值 = 总股本 × 收盘价` |
| A股财务数据无PE | 使用`get_fina_reports`获取TTM净利润，计算`PE = 市值 / 净利润TTM` |
| 港股无财务数据 | 使用`get_stock_mktfin_indicator`获取估值指标 |
| 指数估值数据类型错误 | 添加`pd.to_numeric`类型转换 |

---

## 四、因子计算流程

### 4.1 流程概览

```
1. 认证登录 → panda_data.init_token(username, password)
2. 获取市场情绪 → get_index_indicator() → Market_Z
3. 获取行情数据 → get_stock_daily() / get_hk_daily()
4. 获取财务/估值数据 → get_fina_reports() / get_stock_mktfin_indicator()
5. 计算PE → PE = 市值 / 净利润TTM（A股）或直接获取（港股）
6. 计算Stock_Z → -(PE - PE_mean) / PE_std
7. 计算Factor → Market_Z × Stock_Z
8. 生成Signal → buy(score≥80) / hold(20≤score<80) / sell(score<20)
9. 输出Parquet → 数据库.parquet
```

### 4.2 核心代码逻辑

**市场情绪计算**（factor.py#L357-L367）：
```python
pe_values = pd.to_numeric(index_df[pe_col].dropna(), errors="coerce").dropna().values
pe_mean = np.mean(pe_values)
pe_std = np.std(pe_values)
market_z_score = (current_pe - pe_mean) / pe_std if pe_std > 0 else 0
```

**个股估值计算**（factor.py#L360-L366）：
```python
market_df["stock_z_score"] = -(market_df["pe"] - pe_mean) / pe_std
market_df["factor_value"] = market_df["market_z_score"] * market_df["stock_z_score"]
```

---

## 五、PandaData SDK接口使用

### 5.1 认证方式
```python
import panda_data
panda_data.init_token(username="86手机号", password="密码")
```

### 5.2 使用的接口

| 接口名 | 用途 | 市场 |
|--------|------|------|
| `get_index_indicator` | 获取指数估值（计算Market_Z） | A股 |
| `get_stock_daily` | 获取A股日线行情 | A股 |
| `get_hk_daily` | 获取港股日线行情 | 港股 |
| `get_fina_reports` | 获取财务报表（计算PE TTM） | A股 |
| `get_share_float` | 获取股本数据（计算市值） | A股 |
| `get_stock_mktfin_indicator` | 获取港股估值指标 | 港股 |
| `get_industry_constituents` | 获取行业成分数据 | A股 |

---

## 六、验证与回测结果

### 6.1 验证结果
```
未来函数检测: ✅ PASS
参数敏感性检测: ✅ PASS
样本外检测: ✅ PASS
```

### 6.2 回测结果（A股20日周期）
| 指标 | 值 |
|------|-----|
| IC | 0.0615 |
| RankIC | 0.0671 |
| ICIR | 0.4906 |
| Long-Short收益 | 1.06% |

### 6.3 最终产物统计
| 项目 | 值 |
|------|-----|
| 全市场有效股票 | 611只 |
| A股 | 377只 |
| 港股 | 234只 |
| Buy信号 | 123只 |
| Hold信号 | 367只 |
| Sell信号 | 121只 |

---

## 七、使用方法

### 7.1 环境准备
```bash
pip install panda_data
pip install pandas numpy pyarrow
```

### 7.2 配置账密
编辑 `.env` 文件：
```
PANDA_USERNAME=your_86_phone_number
PANDA_PASSWORD=your_password
```

### 7.3 运行因子计算
```bash
cd 开发产物/scripts
python factor.py --market all
```

### 7.4 运行验证
```bash
python validate.py
```

### 7.5 运行回测
```bash
python backtest.py --market cn
```

---

## 八、输出字段说明

| 字段名 | 说明 | 示例 |
|--------|------|------|
| trade_date | 交易日期 | 20260717 |
| ts_code | 股票代码 | 000001.SZ |
| market | 市场 | cn/hk/us |
| factor_value | 因子值 | 2.35 |
| score | 百分位分数 | 85.5 |
| signal | 信号 | buy/hold/sell |
| confidence | 置信度 | 70 |
| pe | 市盈率 | 15.2 |
| pb | 市净率 | 2.1 |
| market_z_score | 市场情绪Z-score | 1.39 |
| stock_z_score | 个股估值Z-score | -1.75 |

---

## 九、注意事项

1. **认证方式**：必须使用 `panda_data.init_token()`，不支持 `init()` 方法
2. **美股数据**：当前无权限获取美股数据，需联系PandaAI开通权限
3. **负PE过滤**：亏损股PE为负，已过滤，不参与因子计算
4. **账密安全**：运行完成后请清理.env中的账密
5. **数据更新**：每次运行因子计算会覆盖生产产物中的parquet文件

---

## 十、版本历史

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-07-17 | v1.0 | 初始版本，使用mock数据 |
| 2026-07-17 | v1.1 | 切换为官方SDK，修复接口问题 |
| 2026-07-17 | v1.2 | 修复估值去重（P0-1） |
| 2026-07-17 | v1.3 | 修复信号翻转（P0-2） |
| 2026-07-18 | v1.4 | 添加PE>0过滤（P0-3），最终交付版 |

---

**文档创建日期**：2026年7月18日
**创建人**：AI Assistant
**项目路径**：E:\因子skill集\skill-templeton-global-contrarian
