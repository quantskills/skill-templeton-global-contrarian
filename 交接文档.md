# Skill-Templeton-Global-Contrarian 交接文档

## 一、项目概述

### 1.1 项目背景
本项目为 **邓普顿全球价值多因子 V2**（Templeton Global Value Factor V2）技能开发。**v2.0（2026-08-09）已由 V1
逆向价值因子替换为 V2 多因子模型**（用户指令："那就换成这个因子"）。V2 秉承约翰·邓普顿的全球价值理念，但不再依赖
单一"估值偏离度"，而是把**价值（便宜）+ 质量（优质）+ 动量（趋势）**综合成七子因子截面打分。

### 1.2 因子逻辑（V2）
七子因子（EP/BP/SP/股息/ROE/杠杆/动量），各自截面 RANK → 市值中性化 → 按可得子因子集合归一化加权 → SCALE：

$$
\begin{aligned}
&EP=\tfrac{1}{clip(PE,0,200)+1},\ BP=\tfrac{1}{clip(PB,0,20)+1},\ SP=\tfrac{1}{clip(PS,0,50)+1}\\
&DIV=symlog1p(股息率),\ ROE=symlog1p(\tfrac{净利润TTM}{净资产}),\ LEV=\log(clip(\tfrac{总负债}{总资产},0,10)+1)\\
&MOM=symlog1p(12月动量-1月动量),\quad symlog1p(x)=sign(x)\cdot\log(|x|+1)\\
&sub_k=RANK\big(x_{log}-0.1\cdot ZSCORE(\log(市值+1))\big)\\
&raw\_score=0.25\,sub_{ep}+0.20\,sub_{bp}+0.10\,sub_{sp}+0.30\,sub_{div}+0.15\,sub_{roe}-0.10\,sub_{lev}+0.10\,sub_{mom}\\
&factor\_value=SCALE(raw\_score)
\end{aligned}
$$

- **方向"大 = 买"**：`factor_value` 越大 = 越便宜 + 越优质 + 越有动量 → 预期前瞻收益越高。
- **降级归一化**：缺失子因子权重置 0，对剩余子因子按 `w/Σ|w|` 重新归一化（保留杠杆负号），各市场权重绝对值和恒为 1。
- **共享算子**：`RANK/ZSCORE/SCALE/SIGN·LOG/clip` 平台算子已翻译为 pandas，见 [v2_operators.py](scripts/v2_operators.py)，
  由 factor.py / backtest.py / validate.py 三脚本统一复用（`build_subfactors` + `combine_raw_score`）。

> ⚠️ **factor_value 语义随 V1→V2 变更**：V1 = `market_z × 行业相对 z`（量级可达上百）；V2 = `SCALE(raw_score)`
> （截面 z-score，量级约 [-3,3]）。买卖信号由**截面百分位**驱动，`SCALE` 单调、不改排名，信号阈值（80/20）无需调整。

### 1.3 覆盖市场（含降级声明）

不同市场数据权限不同，V2 对港股/美股/动量做了**如实降级**，各市场因子内涵不完全等价：

| 市场 | 可用子因子 | 降级原因 | factor_value 口径 | valuation_method |
|------|-----------|----------|-------------------|------------------|
| **A股（CN）** | EP/BP/SP/股息/ROE/杠杆/动量（完整 7；动量窗口不足退化为 6） | `get_fina_reports` 提供完整财务字段 | `SCALE(7/6 子因子加权)` | `V2_MULTIFACTOR_cn` |
| **港股（HK）** | 仅 EP（若 PB 可得则 EP+BP） | 港股接口仅 PE(±PB)，**无 PS/股息/ROE/杠杆/营收** | `SCALE(EP 或 EP+BP)` | `V2_MULTIFACTOR_hk` |
| **美股（US）** | 无（不走子因子链） | **无 PE/财务权限**，仅日线价格 | `SCALE(−pct_from_low)` 价格代理 | `PRICE_PROXY_52w_low` |

#### 1.3.1 V2 降级设计小节
- **动量降级**：动量需回看 252 交易日。单日截面（如 validate.py）或历史不足 252 日时，动量整列缺失、权重置 0，
  A 股自动退化为 6 子因子。回测采用**长拉短评**：拉 550 日历史保证每评估日可回看 252 日，仅对近 150 日评估 IC；
  若触套餐限额（错误码 600003）则回退 150 日且动量权重置 0。
- **港股降级**：`get_stock_mktfin_indicator` 仅返回 PE（部分含 PB），无其余财务，故港股仅 EP(±BP)。
  港股口径原始 IC 为负，已对港股 `raw_score` **取反**（`market=="hk"` 分支，factor.py 与 backtest.py 一致），
  使方向翻转为正；取反后 IC≈+0.05，仍属弱因子。
- **美股降级**：无 PE/财务权限，退化为 `pct_from_low=(close−滚动低点)/滚动低点` 价格代理，`factor_value=SCALE(−pct_from_low)`。
  该代理存在极端离群（拆股/仙股致低点近 0，比值达数万倍），已做**截面 99% 分位 winsorize**（上限截尾），
  避免 SCALE 产生 −70 级异常 z 值（factor.py US 分支与 backtest.py `_cross_section_v2` 均已处理；处理后量级约 [-7.5, 0.3]）。

---

## 二、文件结构

```
skill-templeton-global-contrarian/
├── 开发产物/
│   ├── .env                    # 环境变量配置（账密模板；环境变量优先、.env 不覆盖）
│   ├── 生产产物/
│   │   └── 数据库.parquet      # 因子计算输出文件（V2，24 列）
│   ├── scripts/
│   │   ├── v2_operators.py     # V2 共享算子 + 子因子构建（三脚本复用）
│   │   ├── factor.py           # 核心因子计算脚本（V2 多因子）
│   │   ├── validate.py         # 验证脚本（三层验证，V2 口径）
│   │   └── backtest.py         # 回测脚本（V2 逐日截面 + 长拉短评）
│   ├── references/
│   │   └── data_guide.md       # PandaData 接口指南
│   ├── SKILL.md                # 技能说明（V2）
│   └── 交接文档.md              # 本文件
```

---

## 三、关键问题与修复记录

### 3.1 P0级问题修复

| 问题编号 | 问题描述 | 根因 | 修复方案 | 修复位置 |
|----------|----------|------|----------|----------|
| P0-1 | 港股估值数据重复 | `get_stock_mktfin_indicator`返回多日期数据 | 添加`drop_duplicates(subset=["ts_code"])` | factor.py |
| P0-2 | 因子符号校准 | 负号方向的样本外 IC 为负、与前瞻收益反向 | 去掉负号：`stock_z_score = (pe - 行业均值) / 行业标准差` | factor.py |
| P0-3 | 负PE股混入buy组 | 亏损股PE为负，被误判为低估 | 添加`market_df["pe"] > 0`过滤 | factor.py |
| P0-4 | 回测 pooled IC 与因子方向相反（旧版） | 对整段多日数据一次性 `groupby(industry)` 求 Z-score，混入跨日期样本、破坏截面语义 | 改为**逐日截面标准化**后纵向拼接（新增 `_cross_section_z`）。修复后 IC 符号随端日/窗口区制变化（20250630→+0.0250，20260717→−0.0249），回测仍判 **FAIL**（\|ICIR\|≈0.16<<0.5） | backtest.py |
| P0-5 | 未来函数检测未做 shift 验证 | 原仅比较指数日期先后 | 新增 shift 边界断言（末尾 period 行必须 NaN）+ 未来/过去收益 IC 差异断言 | validate.py |
| P0-6 | 文档-代码字段不一致 | `factor_id`/`data_version`/附加字段三处不符 | 统一 `factor_id=templeton_global_contrarian`、`data_version=YYYYMMDD_HHMMSS`、字段表按实际输出重写 | factor.py / SKILL.md |

### 3.2 技术难点解决

| 难点 | 解决方案 |
|------|----------|
| A股日线无market_cap | 使用`get_share_float`获取总股本，计算`市值 = 总股本 × 收盘价` |
| A股财务数据无PE | 使用`get_fina_reports`获取TTM净利润，计算`PE = 市值 / 净利润TTM` |
| 港股无财务数据 | 使用`get_stock_mktfin_indicator`获取估值指标 |
| 指数估值数据类型错误 | 添加`pd.to_numeric`类型转换 |

---

## 四、因子计算流程（V2）

### 4.1 流程概览

```
1. 认证登录 → panda_data.init_token(username, password)（环境变量/.env/命令行/交互式）
2. 获取行情数据 → get_stock_daily() / get_hk_daily() / get_us_daily()
3. 按市场准备子因子输入：
   - A股：get_fina_reports 自算 PE/PB/PS/股息/ROE/杠杆 + get_stock_daily 派生 12-1 月动量
   - 港股：get_stock_mktfin_indicator 取 PE(±PB)
   - 美股：日线派生 pct_from_low（99% 分位 winsorize）
4. 构建子因子 → v2_operators.build_subfactors()（各子因子截面 RANK + 市值中性）
5. 降级合成 → combine_raw_score(subs, 可得子因子集合)（按 |w| 归一化加权 → SCALE）
6. factor_value = SCALE(raw_score)（美股 = SCALE(−pct_from_low)）
7. 生成Signal → 截面分位：buy(score≥80) / hold(20≤score<80) / sell(score<20)
8. 输出Parquet → 数据库.parquet（24 列，PK=(trade_date, ts_code)）
```

### 4.2 核心代码逻辑（V2）

**A股子因子输入派生**（[factor.py](file:///e:/因子skill集/skill-templeton-global-contrarian/开发产物/scripts/factor.py#L551-L585)）：
```python
market_df["pe"]        = mcap / net_profit_ttm
market_df["pb"]        = mcap / equity            # equity = bs_total_hldr_eqy_exc_min_int（不含少数）
market_df["ps"]        = mcap / revenue_ttm
market_df["div_yield"] = div_ttm / mcap
market_df["roe"]       = net_profit_ttm / equity
market_df["leverage"]  = total_liab / total_assets
# 12-1 月动量（长拉历史）：close/DELAY(close,252) − close/DELAY(close,21)
available = get_available_subfactors("cn", has_pb=True, has_momentum=has_momentum)
```

**V2 子因子合成**（[v2_operators.py](file:///e:/因子skill集/skill-templeton-global-contrarian/开发产物/scripts/v2_operators.py#L145-L165)）：
```python
subs = build_subfactors(market_df)              # 7 个截面 RANK 子因子（含市值中性）
raw  = combine_raw_score(subs, available)        # 缺失权重置 0 → 按 |w| 归一化 → SCALE
market_df["factor_value"] = op_scale(raw)        # 方向"大=买"
market_df["score"]  = market_df["factor_value"].rank(pct=True) * 100
market_df["signal"] = np.select([score>=80, score<20], ["buy","sell"], default="hold")
```

---

## 五、PandaData SDK接口使用

### 5.1 认证方式
```python
import panda_data
panda_data.init_token(username="86手机号", password="密码")
```

### 5.2 使用的接口

| 接口名 | 用途 | 市场 |
|--------|------|------|
| `get_stock_daily` | 获取A股日线行情（+派生动量） | A股 |
| `get_hk_daily` | 获取港股日线行情 | 港股 |
| `get_us_daily` | 获取美股日线行情（+派生 pct_from_low） | 美股 |
| `get_fina_reports` | 财务报表：净利润/营收/股利/净资产/负债/资产/股本（自算 EP/BP/SP/股息/ROE/杠杆） | A股 |
| `get_share_float` | 股本数据（计算市值） | A股 |
| `get_stock_mktfin_indicator` | 港股估值指标 PE(±PB) | 港股 |
| `get_industry_constituents` | 行业成分数据（参考列 industry） | A股 |
| `get_index_indicator` | 指数估值（`market_z_score` 保留列，V2 不参与合成） | A股 |

---

## 六、验证与回测结果（V2，2026-08-09 实测，如实披露）

### 6.1 验证结果（严格口径，validate.py 三项全过、`sys.exit(0)`）
```
未来函数检测:   ✅ PASS   （shift 末尾20行为NaN；IC(未来20日)=+0.1125 vs IC(过去20日)=+0.0460）
参数敏感性检测: ✅ PASS   （4档阈值IC[+0.141,+0.191,+0.120,+0.242]全同号，极差=0.122<0.15）
样本外检测:     ✅ PASS   （4锚点IC[+0.161,+0.174,+0.0004,+0.169]全为正，衰减49.43%<50%）
```
> 说明：validate.py 严格判据未放宽——参数敏感性要求"IC 全程同号 + 极差<0.15"，样本外要求"每个锚点期方向一致
> （任一反号即 FAIL）+ 前后半段衰减<50%"。V2 三项**全部通过**（V1 曾在参数敏感性/样本外 FAIL）。
> ⚠️ **擦边提示（不粉饰）**：样本外 2024Q3~2025Q1 锚点 IC≈+0.0004 近乎为 0、衰减率 49.43% 贴近 50% 门槛，
> 属**擦边通过**，稳健性余量有限。

### 6.2 回测结果

**A股（20 日周期，`--end-date 20250630`，550 日历史/150 日评估、动量开）——达标（强）：**
| 指标 | 值 | 验收门槛 | 是否达标 |
|------|-----|---------|---------|
| IC（pooled） | **+0.1427** | ≥ 0.03 | ✅ |
| RankIC | **+0.1620** | 显著 >0 | ✅ |
| ICIR | **+1.5867** | ≥ 0.5 | ✅ |
| 平均 IC | +0.1607 | — | 强 |
| Long-Short收益 | **+3.86%** | >0 | ✅（分层近单调 G1<G2<…<G5） |

> **判语（统一口径）**：以 `--end-date 20250630` 为准，A股 pooled IC = **+0.1427**、ICIR = **+1.59**、
> RankIC = **+0.1620** → **回测达标（强）**。相比 V1（IC≈+0.0250、ICIR≈0.16）是**实质改进**。
>
> 📌 **IC 符号仍具区制依赖（务必记录以保证可复现，未挑窗口粉饰）**：**同一份代码**，回测 IC 符号由 **150 日
> 评估窗口的落窗位置**决定：
> - `--end-date 20250630`（落在近端正区制）→ **IC = +0.1427**、ICIR = **+1.59**（本文档采用口径）。
> - `--end-date 20260717`（backtest.py 默认端日，落长窗口负区制）→ **IC = −0.0892**、ICIR = **−0.73**（转负）。
>   两者**同一代码**，差异仅来自端日/窗口，而非 Bug。复现强正结果必须指定 `--end-date 20250630`。
>
> 📌 **口径确认**：`calculate_factor_value` 已实现**逐日截面 V2 合成**（`groupby("date")` → `_cross_section_v2`，
> 见 [backtest.py](file:///e:/因子skill集/skill-templeton-global-contrarian/开发产物/scripts/backtest.py#L257-L270)），
> 与 factor.py/validate.py 完全一致 ✅。回测采用**长拉短评**（550 日历史保证动量回看，仅评估近 150 日）。

**港股/美股（降级市场，`--end-date 20250630`）如实披露：**
| 市场 | IC | RankIC | ICIR | 判语 |
|------|-----|--------|------|------|
| 港股（EP±BP 降级，**已取反**） | **+0.0541** | +0.0269 | +1.37 | **弱正**，取反后方向为正、量级仍弱 |
| 美股（价格代理降级） | **+0.0062** | +0.0303 | +0.96 | **近乎 0**，价格代理信息量极弱 |

> 📌 **港股信号取反（2026-08-09，用户指令"港股的因子信号取反"）**：港股 EP±BP 降级口径下原始 IC 为
> **−0.0541**（方向与前瞻收益相反），故对港股 `raw_score` 取负号，使 `factor_value/score/signal` 一并翻转。
> 取反后港股 IC=**+0.0541**、RankIC=**+0.0269**、ICIR=**+1.37**（严格符号翻转，非挑窗口）。取反仅作用于
> **港股**（factor.py `calculate_factor_for_market` 与 backtest.py `_cross_section_v2` 的 `market=="hk"` 分支），
> A股/美股不受影响。**注意**：港股量级仍弱（|IC|≈0.05），取反解决的是方向、非强度。

> **结论（不粉饰）**：V2 相比 V1 有**实质改进**——三项严格验证首次全部通过；A 股近端正区制回测表现强
> （IC≈0.14、ICIR≈1.59、分层近单调）。但须同时承认：**IC 符号仍区制依赖**（长窗口转负）、**样本外为擦边通过**、
> **港股取反后为弱正（|IC|≈0.05）、美股近 0**。因子强表现集中于 A 股近端区制，跨区制/跨市场稳健性有限。数值均如实记录、未挑窗口粉饰。

### 6.3 最终产物统计（V2，--market all，连续 3 个真实交易日 20260805/06/07 重跑）
| 项目 | 20260805 | 20260806 | 20260807 |
|------|---------|---------|---------|
| 全市场有效股票 | 8451 | 8447 | 8452 |
| A股 | 968 | 968 | 968 |
| 港股 | 1448 | 1448 | 1448 |
| 美股 | 6035 | 6031 | 6036 |
| Buy信号 | 1692 | 1691 | 1692 |
| Hold信号 | 5071 | 5068 | 5071 |
| Sell信号 | 1688 | 1688 | 1689 |

> 产物质检（[数据库.parquet](file:///e:/因子skill集/skill-templeton-global-contrarian/开发产物/生产产物/数据库.parquet)）：
> **25350 行 × 24 列**，`factor_id=templeton_global_value_v2`，主键 (trade_date, ts_code) 唯一（0 重复），
> `factor_value` 无空值。各市场 factor_value 量级：A股 [-2.24, 1.96]、港股 [-2.02, 2.08]、
> 美股 [-7.5, 0.28]（价格代理 99% winsorize 后仍偏斜，属固有局限）。

> 🔒 **凭据安全（v1.5）**：`.env` 已清空为模板（`PANDA_USERNAME=86手机号` / `PANDA_PASSWORD=密码`），
> 凭据不再随包分发；运行时通过环境变量或命令行注入，且 `_load_env_file` 现在**环境变量优先、.env 不覆盖**。

> 🔧 **港股/全市场/多日修复（v1.5）**：
> 1. 港股估值 `get_valuation_data` 改为**按 500 只分片全量拉取**（此前 `symbol[:500]` 截断，2799 只仅 236 只有估值 → 现 1448 只全覆盖）；
> 2. 非交易日（如周日）自动回退最近交易日（`resolve_trade_date`），不再报“所有市场为空”硬错；
> 3. A股股本 `get_share_float` 单日区间返回空 → 改取 **as_of_date 前 10 天区间内最后一条股本**（60 天窗口触套餐限额 600003，10 天窗口合规；A股有效只数 377 → 718）。

---

## 七、使用方法

### 7.1 环境准备
```bash
pip install panda_data
pip install pandas numpy pyarrow
```

### 7.2 配置账密
编辑 `.env` 文件：
```
PANDA_USERNAME=your_86_phone_number
PANDA_PASSWORD=your_password
```

### 7.3 运行因子计算
```bash
cd 开发产物/scripts
python factor.py --market all
```

### 7.4 运行验证
```bash
python validate.py
```

### 7.5 运行回测
```bash
python backtest.py --market cn
```

---

## 八、输出字段说明

输出恰为 **24 列**（PK=(trade_date, ts_code)），列顺序见 [factor.py](file:///e:/因子skill集/skill-templeton-global-contrarian/开发产物/scripts/factor.py#L887-L893)：

| 字段名 | 说明 | 示例 |
|--------|------|------|
| trade_date | 交易日期 | 20260807 |
| asset_type | 资产类型 | stock |
| ts_code | 股票代码 | 000001.SZ |
| market | 市场 | cn/hk/us |
| factor_id | 因子ID（固定） | templeton_global_value_v2 |
| factor_name | 因子名称（固定） | 邓普顿全球价值多因子V2 |
| factor_value | **V2 因子值 = `SCALE(raw_score)`（截面 z-score，越大预期前瞻收益越高；量级约 [-3,3]）** | 1.85 |
| score | 截面百分位分数（0-100） | 85.5 |
| rank | 截面排名 | 42 |
| signal | 信号（score≥80买 / <20卖 / 其余持） | buy/hold/sell |
| confidence | 置信度 | 70 |
| data_version | 数据版本（YYYYMMDD_HHMMSS） | 20260807_153012 |
| update_time | 更新时间 | 2026-08-07 15:30 |
| market_cap | 市值（A股=总股本×收盘价） | 1.2e11 |
| pe | 市盈率（A股自算，港股接口，美股 NaN） | 15.2 |
| pb | 市净率（可得时） | 2.1 |
| industry | 所属行业（参考列） | 银行 |
| industry_pe_avg | 行业平均 PE（保留参考列，V2 不参与合成） | 12.4 |
| market_z_score | 市场情绪 Z-score（保留列，V2 不参与合成） | 1.02 |
| stock_z_score | **承载 V2 `raw_score`（合成原始分，未 SCALE）** | 0.63 |
| close | 收盘价 | 12.35 |
| valuation_method | 估值/合成口径 | V2_MULTIFACTOR_cn / V2_MULTIFACTOR_hk / PRICE_PROXY_52w_low |
| pct_from_low | 距 52 周低点比率（美股价格代理用；已 99% winsorize） | 0.15 |
| valuation_metric | 估值指标标签 | pe / pct_from_low |

> ⚠️ **V1→V2 语义变更**：`factor_value` 由 V1 的 `market_z × 行业相对 z`（量级可达上百）改为
> V2 的 `SCALE(raw_score)`（截面 z-score，约 [-3,3]）；`stock_z_score` 由「行业相对个股估值 z」改为
> 承载「V2 合成原始分 raw_score」；`market_z_score`/`industry_pe_avg` 降级为**保留参考列，不再参与因子合成**。

---

## 九、注意事项

1. **认证方式**：必须使用 `panda_data.init_token()`，不支持 `init()` 方法
2. **凭据安全**：凭据仅经环境变量/`.env` 注入，**环境变量优先、.env 不覆盖**；源码与产物中无硬编码凭据；`.env` 为占位模板
3. **美股为价格代理降级**：无 PE/财务权限，`factor_value=SCALE(−pct_from_low)`（52 周低点代理），信息量弱、已做 99% winsorize，详见 §1.3.1
4. **港股为 EP(±BP) 降级**：`get_stock_mktfin_indicator` 仅 PE(±PB)，无 PS/股息/ROE/杠杆；港股信号已取反（v2.1），取反后回测为正，详见 §6.2
5. **务必指定 `--end-date`**：回测 IC 符号**区制依赖**（同一代码，20250630 落正区制、默认 20260717 落负区制），复现强正结果须 `--end-date 20250630`，详见 §6.2
6. **动量长拉短评**：回测拉 550 日历史仅评估近 150 日；单日截面/窗口不足则动量权重置 0（A股退化为 6 子因子）
7. **数据更新**：每次运行因子计算会覆盖生产产物中的 parquet 文件（24 列，PK 唯一）

---

## 十、版本历史

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-07-17 | v1.0 | 初始版本，使用mock数据 |
| 2026-07-17 | v1.1 | 切换为官方SDK，修复接口问题 |
| 2026-07-17 | v1.2 | 修复估值去重（P0-1） |
| 2026-07-17 | v1.3 | 修复信号翻转（P0-2） |
| 2026-07-18 | v1.4 | 添加PE>0过滤（P0-3），最终交付版 |
| 2026-08-08 | v1.5 | 凭据清理（.env 模板+环境变量优先）、港股 500 分片全量、非交易日回退、A股股本 10 日区间；validate 严格化（跳过即 FAIL）；回测逐日截面 + 区制依赖记录 |
| 2026-08-09 | **v2.0** | **因子由 V1 逆向价值替换为 V2 全球价值多因子**（7 子因子 EP/BP/SP/股息/ROE/杠杆/动量 → 截面 RANK → 市值中性 → 归一化加权 → SCALE）；新增 `v2_operators.py` 共享算子；三市降级声明（A股 7/6、港股 EP±BP、美股价格代理+99% winsorize）；`factor_id=templeton_global_value_v2`、仍 24 列；validate 三项全 PASS；A股回测（20250630）IC+0.1427/ICIR+1.59（强），但区制依赖仍在、港股负/美股≈0，如实披露 |
| 2026-08-09 | **v2.1** | **港股信号取反**（用户指令）：港股 `raw_score` 取负，factor.py/backtest.py `market=="hk"` 分支一致；港股回测 IC 由 −0.0541 → **+0.0541**（符号翻转），量级仍弱；A股/美股不受影响 |

---

**文档创建日期**：2026年7月18日
**创建人**：AI Assistant
**项目路径**：E:\因子skill集\skill-templeton-global-contrarian
